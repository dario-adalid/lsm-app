import 'package:flutter/material.dart';
import 'package:mobile_app/style.dart';

class Shared {
  static final AppBar appBar = AppBar(
    title: const Text("LSM APP"),
    backgroundColor: LsmColors.color3,
  );
}